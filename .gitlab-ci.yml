variables:
  MAVEN_OPTS: -Xmx256m -XX:MaxPermSize=512m

stages:
  - build
  - test

maven-build:
  image: maven:3-jdk-8
  stage: build
  script:  mvn clean compile -B

maven-test:
  image: maven:3-jdk-8
  stage: test
  script: mvn test -B