image: maven:3-jdk-8

variables:
  MAVEN_OPTS: -Xmx256m -XX:MaxPermSize=512m

stages:
  - build
  - test

maven-build:
  stage: build
  script:
    - mvn clean compile -B

maven-test:
  stage: test
  dependencies:
    - maven-build
  script:
    - mvn test -B